language: node_js
node_js:
  - 11
  - 10
  - 9
jdk:
  - oraclejdk8
addons:
  sonarcloud: true
script:
  - sonar-scanner -Dsonar.login=$SONAR_TOKEN -Dsonar.organization=fMercury-github -Dsonar.projectKey=ciclo -Dsonar.sources='./app' -X
